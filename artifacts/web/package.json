{
  "name": "@workspace/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd ../mobile && EXPO_PUBLIC_DOMAIN=$REPLIT_DEV_DOMAIN EXPO_PUBLIC_REPL_ID=$REPL_ID pnpm exec expo start --web --port $PORT --host lan",
    "build": "cd ../mobile && pnpm exec expo export --platform web --output-dir ../web/dist",
    "serve": "node serve.mjs",
    "typecheck": "echo 'web artifact uses mobile codebase'"
  },
  "devDependencies": {
    "@workspace/mobile": "workspace:*"
  }
}
